{
  "name": "logistic-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "Cross-border logistics tracking platform",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter @logistic/web dev",
    "dev:api": "pnpm --filter @logistic/api dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
